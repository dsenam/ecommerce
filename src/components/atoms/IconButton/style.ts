import styled from "styled-components";

export const IconButtonStyled = styled.button<{ $primary?: boolean }>`
  border: 0;
  border-radius: 3em;
  cursor: pointer;
  display: inline-block;
  line-height: 1;
  font-size: 1rem;
  padding: 0.6rem 1rem;
`;
